apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  namespace: default
  name: static-backend
spec:
  static:
    host: example.com
    port: 8888
  policies:
    auth:
      # TODO: kubebuilder:validate allows this but `translateBackendAuth` exepects "inline key or secretRef"
      passthrough: {}
---
# Output
output: []
status:
  conditions:
  - lastTransitionTime: fake
    message: 'failed to translate backend: backend auth requires either inline key
      or secretRef'
    reason: TranslationError
    status: "False"
    type: Accepted
